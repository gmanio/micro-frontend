export type LayoutMode = "auto" | "mobile" | "desktop";
export type ResolvedLayout = "mobile" | "desktop";

/** Default desktop breakpoint (min-width), matches Tailwind `md`. */
export const DEFAULT_DESKTOP_MIN_WIDTH_PX = 768;
